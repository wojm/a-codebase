/* eslint-disable no-constant-condition */

/**
 * Ternary opertator helps us take one of two different possibilities based on a conditional
 */

const first = true? 'first': 'second';

console.log(first); // first

const second = false? 'first': 'second';
console.log(second); // second

